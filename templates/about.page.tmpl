{{template "base" .}}
{{define "content"}}
    <div class="container">
        <div class="row">
          <h1>This is About Page!</h1>
          <p>This is some text</p>
          <p>This came from the template: {{ index .StringMap "test" }}</p>
          </div>
      </div>
{{end}}